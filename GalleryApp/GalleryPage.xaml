<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:GalleryApp.Views.Controls"
             x:Class="GalleryApp.GalleryPage">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>

        <HorizontalStackLayout Grid.Row="0" HorizontalOptions="Center">
            <Button Text="Feed" TextColor="White" BackgroundColor="Transparent" Command="{Binding ShowFeedCommand}"/>
            <Label Text="|" TextColor="White" FontSize="20" VerticalTextAlignment="Center"/>
            <Button Text="Favourites" TextColor="White" BackgroundColor="Transparent" Command="{Binding ShowFavouritesCommand}"/>
        </HorizontalStackLayout>
        
        <CollectionView Grid.Row="1" ItemsSource="{Binding DisplayedPhotos}" 
                        RemainingItemsThreshold="5"
                        RemainingItemsThresholdReachedCommand="{Binding LoadNextPageCommand}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2"
                                 HorizontalItemSpacing="5" VerticalItemSpacing="5"/>
            </CollectionView.ItemsLayout>
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <controls:PhotoCard/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
